<!DOCTYPE html>
<html>

<head>
  <link href="https://cdn.quilljs.com/1.3.6/quill.snow.css" rel="stylesheet">
  <title>HeavenJosun</title>
  <link rel='stylesheet' href='/stylesheets/style.css' />
</head>

<body>
  <div class="_1VP69d9lk-Wk9zokOaylL " style="--background:#DAE0E6;--canvas:#DAE0E6">
    <%- include ('header') %>
    <form action="/feeds" method="POST"> 
      <div style="display:flex">
        <div
          style="padding:10px; margin-left:200px; margin-top:80px; width:660px; height:580px; border: 1px solid #EDEFF1; background-color:#FFFFFF; border-radius: 10px;">
          <div style="height:50px; margin-bottom: 10px;">
            <input name="title" type="text" class="text-input" style="background-color: #FFFFFF; height:100%; width:100%"
              placeholder="제목" id="title">
          </div>
          <select class="form-control form-control-sm" name="tag_id">
            <% for (var i = 0; i < tags.length; i++) { %>
              <option value="<%= tags[i].id %>">
                <%= tags[i].tagname %>
              </option>
            <% } %>
          </select>
          <textarea id="summernote" name="body"></textarea>
          <div style="height:40px;margin-top:0px; display:inline-flex; align-items: center; width:100%;">
            <div style="width:70%">
            </div>
            <button onclick="getData()" class="button signup-button" style="width:30%; height:40px;">
              글 쓰기
            </button>
          </div>
        </div>
      </div>
    </form>
  </div>

</html>
<link href="https://cdn.jsdelivr.net/npm/summernote@0.8.18/dist/summernote.min.css" rel="stylesheet">
<script src="https://cdn.jsdelivr.net/npm/summernote@0.8.18/dist/summernote.min.js"></script>

<script>
  function getData() {
    var text = quill.getText();
    console.log(text);
  }

  $(document).ready(function() {
  $('#summernote').summernote({
        placeholder: '본문을 입력하시오',
        tabsize: 2,
        height: 300,
        minHeight: 300,             // set minimum height of editor
        maxHeight: 300,             // set maximum height of editor
  });
});
</script>